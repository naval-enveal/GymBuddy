gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Vendored Meta DAT SDK artifacts for the glasses release channel — proprietary,
# never committed. Dropped into app/libs/ only for the glasses-channel build.
app/libs/*.aar
app/libs/*.jar
